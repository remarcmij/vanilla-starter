/**
 * If your WEB API requires credentials (e.g. an API key) you can rename this
 * file to `.credentials.js` and define the credentials here as constants.
 *
 * The file `.credential.js` is ignored by git (through file `.gitignore`)
 */

const API_KEY = null;
